from fastapi import FastAPI
app = FastAPI(
    name = "procesos_estocasticos"
    description = "Una api con algunos algoritmos para facilitar el analisis de cadenas de markov"
    version = "0.0.0"
)